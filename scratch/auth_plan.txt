1. We need a `performUserLogin` function in `authService.ts`.
2. It should accept `{ username, password, fingerprint, ip, userAgent }`.
3. It will return an object with `{ type: 'SUCCESS' | 'COMPROMISED' | 'NEEDS_ACTIVATION' | 'NEEDS_MIGRATION' | 'RATE_LIMITED' | 'INVALID_CREDENTIALS', ...data }`
