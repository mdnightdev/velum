Open src/views/UserWorkspace/UserSidebar.tsx. I want to lock the 'Market', 'Notifications', 'Wallet', and 'Saved Notes' sidebar items without removing them from the UI.
​Please update their specific onClick handlers to prevent navigation. Instead, when clicked, trigger a sleek UI toast or alert that says ' Feature in development. Coming soon!'.
​Also, apply a Tailwind CSS class like opacity-50 and cursor-not-allowed to only these specific items so they visually look inactive, but keep the UI looking full and professional.""

same as appearance in settings 
